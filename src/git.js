/**
 * Git preflight, snapshots, content fingerprints, remote-configuration guards,
 * and Worker commit authorization/verification belong here. Remote mutation is
 * forbidden in V1.
 */

export {};
